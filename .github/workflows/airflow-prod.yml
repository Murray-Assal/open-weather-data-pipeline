name: Run Airflow Pipeline

on:
  workflow_dispatch:   # manually trigger
  schedule:
    - cron: "0 3 * * *"  # every day at 3 AM UTC (optional)

jobs:
  run-pipeline:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: airflow
          POSTGRES_PASSWORD: airflow
          POSTGRES_DB: airflow
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U airflow"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Docker Compose
        uses: docker/setup-buildx-action@v3

      - name: Build and start Airflow stack
        run: |
          docker compose up -d --build
          sleep 60  # wait for services to be ready

      - name: Start Airflow stack
        run: |
          docker compose --profile debug up -d --build
          echo "Waiting for Airflow to parse DAGs..."
          sleep 90
      
      - name: List available DAGs
        run: docker compose run --rm airflow-cli airflow dags list
      
      - name: Trigger DAG
        run: docker compose run --rm airflow-cli airflow dags trigger open_weather_map_dag

      - name: View logs (optional)
        run: docker compose logs

      - name: Shutdown stack
        if: always()
        run: docker compose down -v
